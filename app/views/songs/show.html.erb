<h1><%= @song.title %></h1>
<h3><%= link_to @song.artist.name, @song.artist %></h3>

<%= button_to "Add to Playlist", new_playlist_song_path, method: :get, params: { song_id: @song.id } %>
<br>
<%= button_to "View All Songs", songs_path, method: :get %>
