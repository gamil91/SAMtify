<h1><%= @artist.name %></h1>
<!-- image -->
<p>Songs: <%= @songs.count %></p>
<ul>
    <% @songs.each do |song| %>
        <li><%= link_to song.title, song %></li>
    <% end %>
</ul>
<%= button_to "View All Artists", artists_path, method: :get %>